import React from "react";
import { Container, Nav, Row, Col } from "react-bootstrap";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faCut } from "@fortawesome/free-solid-svg-icons";

class Sidebar extends React.Component {
  constructor(props) {
    super(props);
    this.state = { activeItems: [] };
    console.log(props.items);
    this.setState(prevState => {
      activeItems: [true, true]
    }
    )
    //props.items.forEach(() => {
      //this.setState(prevState => ({
        //activeItems: [...prevState.activeItems, true]
      //}));
    //});
    console.log(this.state.activeItems);
  }

  setActive = id => {

  };

  render() {
    return (
      <div id="sidebar">
        <Container fluid className="flex-column">
          <Row>
            <Col sm={12}>
              <div className="sidebar-header">
                <FontAwesomeIcon className="cut-icon fa-2x" icon={faCut} />
                <h3>express cuts</h3>
              </div>
            </Col>
          </Row>
          <Row>
            <Nav>
              <div className="justify-content-center ml-3">
                {this.props.items.map(item => (
                  <Row className="itemRow">
                    <Nav.Item className="navitem">
                      <Nav.Link
                        className={
                          "sidebar-items " +
                          (this.state.activeItems[item.id] && "active")
                        }
                        onClick={this.setActive(item.id)}
                        href={item.link}
                      >
                        {item.name}
                        {<hr />}
                      </Nav.Link>
                    </Nav.Item>
                  </Row>
                ))}
              </div>
            </Nav>
          </Row>
        </Container>
      </div>
    );
  }
}

export default Sidebar;
